module.exports = {
  PORT: process.env.PORT || 8000,
  NODE_ENV: process.env.NODE_ENV || 'development',
  DATABASE_URL: process.env.DATABASE_URL || 'https://git.heroku.com/still-hamlet-81403.git',
  // CLIENT_ORIGIN: 
}
